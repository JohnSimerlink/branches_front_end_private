export default class ContentItemUsersStats {
    // public static get({contentItemId, userId}) {
    //     if (!contentItemId) {
    //         throw new Error('ContentItemUsersStats.ts Error! contentItemId empty')
    //     }
    //     if (!userId) {
    //         throw new Error('ContentItemUsersStats.ts Error! userId empty')
    //     }
    //     return new Promise((resolve, reject) => {
    //         const lookupKey = 'content/' + contentItemId + '/userStats/' + userId
    //         firebase.database.ref(lookupKey).once('value', (snapshot) => {
    //             const userStats = snapshot.val()
    //             resolve(userStats)
    //         })
    //     })
    // }
    // public static create({contentItemId,})
}
