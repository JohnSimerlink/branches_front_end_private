import Vue from 'vue'
import Vuex from 'vuex'
Vue.use(Vuex)

const state = {
    uri: null,
};

const getters = {
}
const mutations = {
}
const actions = {}

const store = new Vuex.Store({
    state,
    mutations,
    actions,
    getters,
})
